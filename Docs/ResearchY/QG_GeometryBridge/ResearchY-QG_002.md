# ResearchY-QG_002 — Distinguishability → Geometry Audit

**Program:** ResearchY — Wave Geometry Program
**Group:** QG — Geometry Bridge
**ID:** ResearchY-QG_002 (permanent)
**Title:** Distinguishability → Geometry Audit
**Status:** COMPLETE
**Date:** 2026-08-30
**File:** `QG_GeometryBridge/ResearchY-QG_002.md`
**Depends on:** ResearchY-D_039 (Difference = distinguishability), NP_018
(distinguishability observable), NP_020 (black hole information), NP_021 (information
horizon), QG_001 (information–geometry bridge)
**Test suite:** `AT.Tests/ResearchY/QG_GeometryBridge/Y_QG_002_Tests.cs`

---

## Purpose

**Can geometry be reconstructed directly from distinguishability?** QG_001 established
that information and geometry share ρ (the count density). This audit goes one step
further: it asks whether spacetime geometry is a MANIFESTATION of distinguishability —
whether the metric can be inferred from the distinguishability structure alone.

---

## 1. Definitions

| Term | Definition |
|---|---|
| **distinguishability** | the 95-state structure (D_039) — N=96 states minus the trivial |
| **information** | I = KL(ρ‖uniform) = I_occ = 0.7513 nats (QG228) |
| **geometry** | the metric g = ρ^(2/d)η (QG197) |
| **metric** | the geometric object generated by the count density ρ |

---

## 2. The direct mapping: distinguishability → geometry

```
Distinguishability (N=96 state structure, D_039)
 → Spectrum (λ_k = 2−2cos(2πk/N))
 → ρ (count density: the occupancy of the modes)
 → Metric g = ρ^(2/d)η (QG197)
 → Einstein tensor (QG222)
```

**The state count determines the spectrum, which determines ρ, which determines the
metric.** The mapping is direct: the geometry is a function of the distinguishability
structure (N=96).

---

## 3. Can metric information be inferred from distinguishability alone?

**YES.** The chain is fully determined by the state structure:

| Input (distinguishability) | Output (geometry) |
|---|---|
| N = 96 (state count) | the spectrum λ_k = 2−2cos(2πk/N) |
| the spectrum | the count density ρ (mode occupancy) |
| ρ | the conformal factor ρ^(2/d) of the metric g = ρ^(2/d)η |
| g | the Einstein tensor (QG222) |

**Metric information (the conformal factor) is inferable from the distinguishability
structure alone** — the metric is a pure function of the spectrum, which is a pure
function of N=96.

---

## 4. Test: horizon formation, ΩΛ, Ωm, measurement structure

| Observable | Distinguishability side | Geometry side |
|---|---|---|
| **horizon formation** | the state structure persists (D_039) | horizon from the metric (g); information conserved (NP_020/021) |
| **ΩΛ** | I_occ/ln K = 0.6839 (information) | the same ρ generates the geometry (QG_001) |
| **Ωm** | 0.3161 (complement) | 1 − ΩΛ |
| **measurement structure** | reveals pre-existing distinguishability (M_005) | the read resolves ρ |

**Every test is consistent: the geometry is a manifestation of the distinguishability
structure via ρ.**

---

## 5. Geometry-first vs information-first

| | Geometry-first | Information-first (AT) |
|---|---|---|
| starting point | an external metric | distinguishability (D_039) |
| derived | ρ from the metric | spectrum → ρ → metric |
| primitives | geometry assumed | Difference → distinguishability |
| direction | metric → ρ → information | **information → ρ → metric** |

**AT is INFORMATION-FIRST: distinguishability is the primitive, and the geometry is
derived from it (via the spectrum and ρ). Geometry is a manifestation of
distinguishability, not its source.**

---

## Theorem

> **Theorem (QG_002).** Spacetime geometry is a MANIFESTATION of distinguishability:
> the metric can be reconstructed directly from the state structure. Proof: (1)
> Distinguishability is the 95-state structure (D_039) — N=96 minus the trivial. (2)
> The state count determines the spectrum: λ_k = 2−2cos(2πk/N) (the C96 ring). (3) The
> spectrum determines the count density ρ (the mode occupancy structure). (4) The
> count density determines the metric: g = ρ^(2/d)η (QG197), with dynamics ∂_t g =
> (2/d)(∂_t ρ/ρ)·g (QG222). (5) Therefore the metric — and hence the geometry — is a
> PURE FUNCTION of the distinguishability structure: N → spectrum → ρ → g. (6) Tests:
> horizon formation (metric from g, information conserved, NP_020/021), ΩΛ = I_occ/ln K
> = 0.6839 (information and geometry share ρ, QG_001), measurement (resolves ρ; reveals
> pre-existing distinguishability, M_005) — all consistent. (7) COMPARISON: AT is
> INFORMATION-FIRST (distinguishability → spectrum → ρ → metric), not geometry-first
> (which would assume an external metric). Classification: distinguishability DERIVED
> (D_039, the primitive structure); the spectrum DERIVED (from N); the count density ρ
> DERIVED; the metric g = ρ^(2/d)η DERIVED (QG197); the geometry as a manifestation of
> distinguishability DERIVED; the information content I = KL(ρ‖uniform) DERIVED
> (QG228). No new primitive; canonical AT unchanged.
>
> *Proof sketch.* (1) Define the chain (Section 2). (2) Show metric information is
> inferable from distinguishability alone (Section 3, verified: N → spectrum → ρ → g).
> (3) Test the observables (Section 4, verified: ΩΛ = 0.6839). (4) Compare information-
> first vs geometry-first (Section 5). ∎

---

## Dependency Graph

```
Difference
 → Distinguishability (D_039: 95 states)
 → Spectrum (λ_k = 2−2cos(2πk/N))
 → ρ (count density)
 → Information (I = KL(ρ‖uniform) = I_occ, QG228)
 → Geometry (g = ρ^(2/d)η, QG197)
 → Gravity (Einstein tensor, QG222)
```

---

## 6. Falsification paths

| Claim | Falsification |
|---|---|
| geometry is a function of distinguishability | a metric deviation from g = ρ^(2/d)η with the same N=96 spectrum |
| the mapping is direct | a geometry not determined by the spectrum (an independent geometric input) |
| information-first | a measurement of the metric that does not pass through ρ |

---

## 7. Counterexamples

| Attempt | Why it fails |
|---|---|
| "Geometry generates distinguishability" | the metric is a derived function of ρ; distinguishability (N=96) is the primitive |
| "The metric is an external input" | g = ρ^(2/d)η follows from the spectrum (QG197) — no external metric needed |
| "Information and geometry are independent" | both share ρ (QG_001) — they are two faces of the same object |
| "The state count does not fix the geometry" | N=96 → spectrum → ρ → g is a pure functional chain |

---

## Classification

| Component | Status |
|---|---|
| distinguishability (95-state structure) | **DERIVED** (D_039, the primitive structure) |
| spectrum | **DERIVED** (from N) |
| count density ρ | **DERIVED** |
| metric g = ρ^(2/d)η | **DERIVED** (QG197) |
| geometry as a manifestation of distinguishability | **DERIVED** |
| information I = KL(ρ‖uniform) | **DERIVED** (QG228) |

**Spacetime geometry is a manifestation of distinguishability: N → spectrum → ρ →
metric is a direct, pure functional chain. AT is information-first. No new primitive;
canonical AT unchanged.**

---

## Open Problems

1. **Per-mode geometric structure (QG_002 OP1).** Whether individual modes of ρ carry
   finer geometric structure than the global conformal factor (extends QG_001 OP1).

---

## Next Steps

- **Registry note:** the geometry is reconstructible from the distinguishability
  structure alone; AT is information-first.

---

## Result Summary

**Test suite:** `AT.Tests/ResearchY/QG_GeometryBridge/Y_QG_002_Tests.cs`
**Run:** 2026-08-30 · **Result:** see `Tests/Results/Y_QG_002_Result.md`

| Test | Verifies | Result |
|---|---|---|
| `Y_QG_002_DistinguishabilityGeometry` | N → spectrum → ρ → g chain | ✅ |
| `Y_QG_002_MetricRelation` | g = ρ^(2/d)η | ✅ |
| `Y_QG_002_InformationBridge` | information and geometry share ρ | ✅ |
| `Y_QG_002_HorizonStructure` | information conserved across the horizon | ✅ |
| `Y_QG_002_CosmologyRelation` | ΩΛ = I_occ/ln K = 0.6839 | ✅ |
| `Y_QG_002_Run` | research report | ✅ |

**Conclusion:** Spacetime geometry is a manifestation of distinguishability: the
metric g = ρ^(2/d)η is a pure function of the N=96 state structure (N → spectrum → ρ →
g). AT is information-first — distinguishability is the primitive, geometry is derived.
No new primitive; canonical AT unchanged.

**Reproduction:** `dotnet test AT.Tests/AT.Tests.csproj --filter "FullyQualifiedName~Y_QG_002"`

---

## References

- ResearchY-D_039 (Difference = distinguishability), NP_018 (distinguishability
  observable), NP_020 (black hole information), NP_021 (information horizon), QG_001
  (information–geometry bridge).
- AT-QG: QG197 (metric ansatz), QG222 (native metric dynamics), QG228 (information),
  QG234 (ΩΛ = I_occ/ln K).
