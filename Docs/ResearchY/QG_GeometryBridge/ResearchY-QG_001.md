# ResearchY-QG_001 — Information–Geometry Bridge Audit

**Program:** ResearchY — Wave Geometry Program
**Group:** QG — Geometry Bridge (new)
**ID:** ResearchY-QG_001 (permanent)
**Title:** Information–Geometry Bridge Audit
**Status:** COMPLETE
**Date:** 2026-08-30
**File:** `QG_GeometryBridge/ResearchY-QG_001.md`
**Depends on:** ResearchY-D_039 (Difference = distinguishability), M_005 (information
conservation), NP_018 (distinguishability observable), NP_020 (black hole
information), NP_021 (information horizon)
**Test suite:** `AT.Tests/ResearchY/QG_GeometryBridge/Y_QG_001_Tests.cs`

---

## Purpose

**How does distinguishability become geometry?** AT derives information from
distinguishability (D_039/M_004) and derives gravity from the count density ρ
(QG222: g = ρ^(2/d)η). This audit finds the common object connecting information and
geometry, and determines the direction of the bridge.

---

## 1. Definitions

| Term | Definition |
|---|---|
| **information** | the distinguishability content: I = KL(ρ‖uniform) = I_occ = 0.7513 nats (QG228) |
| **distinguishability** | the 95-state structure (D_039) |
| **geometry** | the metric g = ρ^(2/d)η (conformal ansatz, QG197) |
| **metric** | the geometric object generated by the count density ρ |
| **spacetime** | the emergent manifold whose dynamics is the actualization flow (QG222) |

---

## 2. Trace both chains

```
Chain 1 (information):
Difference → Distinguishability (D_039) → Information (I = KL(ρ‖uniform), QG228)

Chain 2 (geometry):
Difference → Actualization → ρ (count density) → g = ρ^(2/d)η (QG197) → Einstein tensor (QG222)
```

**BOTH chains pass through the SAME object: ρ, the count density.**

---

## 3. The common object

| Object | Geometry side | Information side |
|---|---|---|
| **ρ (count density)** | g = ρ^(2/d)η — the metric's conformal factor (QG197/222) | I = KL(ρ‖uniform) — the information content (QG228) |
| **dynamics** | ∂_t g = (2/d)(∂_t ρ/ρ)·g (QG222) | ΩΛ = I_occ/ln K = 0.6839 (QG234) |

**ρ is the common object: it generates the metric (geometry) and the KL-divergence
(information).**

---

## 4. Analysis: count density, information density, closure, horizon bookkeeping

| Quantity | Geometry | Information |
|---|---|---|
| **count density ρ** | the metric's conformal factor (g = ρ^(2/d)η) | the distribution whose KL-divergence is I_occ |
| **information density I_occ** | feeds ΩΛ (cosmology) | IS the information (KL(ρ‖uniform)) |
| **closure structure** | the spectrum determines the geometry | the spectrum determines the state space |
| **horizon bookkeeping** | the metric defines the horizon | information conserved across it (NP_020/021) |

**Every object that appears in geometry also appears in information — they share ρ.**

---

## 5. Determination

| Option | Verdict |
|---|---|
| A) information generates geometry | **NO** — information (KL) is a derived property of ρ, not the source of ρ |
| B) geometry generates information | **NO** — the metric is a derived property of ρ, not the source of ρ |
| C) both emerge from a deeper object | **YES — ρ, the count density of Actualization, generates BOTH** |
| D) no bridge | **NO** — the bridge is ρ |

**Both information and geometry EMERGE from the count density ρ — which itself is
generated by Actualization. Neither is the source of the other; they are two faces of
the same ρ.**

---

## 6. Test: black holes, ΩΛ, Ωm, measurement

| Observable | Geometry side | Information side |
|---|---|---|
| **black holes** | horizon from the metric (g) | information conserved across it (NP_020/021) |
| **ΩΛ** | 0.6839 (from ρ's KL divergence) | I_occ/ln K (information density) |
| **Ωm** | 0.3161 (complement) | 1 − ΩΛ |
| **measurement** | the read resolves ρ | reveals the pre-existing distinguishability (M_005) |

**Every test confirms: ρ underlies both the geometry and the information.**

---

## Theorem

> **Theorem (QG_001).** Both geometry and information EMERGE from the SAME deeper
> object — the count density ρ generated by Actualization. Proof: (1) The metric is
> g = ρ^(2/d)η (QG197), with dynamics ∂_t g = (2/d)(∂_t ρ/ρ)·g (QG222): geometry is a
> function of ρ. (2) The information content is I = KL(ρ‖uniform) = I_occ = 0.7513
> nats (QG228): information is a function of ρ. (3) The cosmological fractions follow
> from the information density: ΩΛ = I_occ/ln K = 0.6839 (QG234). (4) Therefore the
> bridge is the count density: Difference → Actualization → ρ → {g = ρ^(2/d)η
> (geometry), I = KL(ρ‖uniform) (information)}. (5) Determination: A) information
> generates geometry — NO (KL is a derived property of ρ); B) geometry generates
> information — NO (the metric is a derived property of ρ); C) BOTH EMERGE FROM ρ —
> YES; D) no bridge — NO. (6) Tests: black holes (horizon from g; information
> conserved across it, NP_020/021), ΩΛ/Ωm (from ρ's KL divergence), measurement
> (resolves ρ; reveals pre-existing distinguishability, M_005) — all confirm the
> shared ρ. Classification: ρ is DERIVED (the actualization count density); the
> metric g = ρ^(2/d)η is DERIVED (QG197); the information I = KL(ρ‖uniform) is
> DERIVED (QG228); the spacetime dynamics is EMERGENT (QG222); the bridge itself (ρ as
> the common object) is DERIVED. No new primitive; canonical AT unchanged.
>
> *Proof sketch.* (1) Trace both chains to ρ (Section 2). (2) Identify the common
> object (Section 3, verified: g = ρ^(2/d)η; I = KL(ρ‖uniform)). (3) Analyze the
> shared quantities (Section 4). (4) Determine C (Section 5). (5) Test (Section 6,
> verified: ΩΛ = 0.6839). ∎

---

## Dependency Graph

```
Difference
 → Actualization
 → ρ (count density) — THE BRIDGE
    → Geometry: g = ρ^(2/d)η (QG197) → Einstein tensor (QG222)
    → Information: I = KL(ρ‖uniform) = I_occ (QG228)
       → ΩΛ = I_occ/ln K = 0.6839 (QG234)
    → Black holes: horizon from g; information conserved (NP_020/021)
 → Gravity (both from ρ)
```

---

## 7. Falsification paths

| Claim | Falsification |
|---|---|
| geometry is a function of ρ | a metric deviation from g = ρ^(2/d)η with the same ρ |
| information is a function of ρ | an information content ≠ KL(ρ‖uniform) |
| both share ρ | a geometry or information source independent of the count density |

---

## 8. Counterexamples

| Attempt | Why it fails |
|---|---|
| "Information generates geometry" | information (KL) is a property OF ρ; the metric is also a property of ρ — ρ is the source, not information |
| "Geometry generates information" | the metric is a derived function of ρ; it does not produce the KL divergence |
| "No bridge exists" | both chains provably pass through ρ (QG197/QG222/QG228) |

---

## Classification

| Component | Status |
|---|---|
| count density ρ | **DERIVED** (the actualization count density) |
| metric g = ρ^(2/d)η | **DERIVED** (QG197) |
| information I = KL(ρ‖uniform) | **DERIVED** (QG228) |
| spacetime dynamics (actualization flow) | **EMERGENT** (QG222) |
| the bridge (ρ as the common object) | **DERIVED** |

**The information–geometry bridge is the count density ρ: geometry (g = ρ^(2/d)η)
and information (I = KL(ρ‖uniform)) are two faces of the same actualization object.
No new primitive; canonical AT unchanged.**

---

## Open Problems

1. **Per-mode ρ geometry (QG_001 OP1).** Whether the per-mode structure of ρ generates
   finer geometric detail (the current bridge uses the global count density).

---

## Next Steps

- **Registry note:** the bridge between information and geometry is ρ — the count
  density that generates both the metric and the KL-divergence.

---

## Result Summary

**Test suite:** `AT.Tests/ResearchY/QG_GeometryBridge/Y_QG_001_Tests.cs`
**Run:** 2026-08-30 · **Result:** see `Tests/Results/Y_QG_001_Result.md`

| Test | Verifies | Result |
|---|---|---|
| `Y_QG_001_InformationGeometry` | I = KL(ρ‖uniform) | ✅ |
| `Y_QG_001_InformationDensity` | I_occ = 0.7513 nats | ✅ |
| `Y_QG_001_MetricRelation` | g = ρ^(2/d)η | ✅ |
| `Y_QG_001_BlackHoleBridge` | information conserved across the horizon | ✅ |
| `Y_QG_001_CosmologyBridge` | ΩΛ = I_occ/ln K = 0.6839 | ✅ |
| `Y_QG_001_Run` | research report | ✅ |

**Conclusion:** Both geometry and information EMERGE from the count density ρ
(Actualization): the metric is g = ρ^(2/d)η (QG197/222) and the information is
I = KL(ρ‖uniform) = 0.7513 nats (QG228), feeding ΩΛ = 0.6839 (QG234). The bridge is
ρ; neither information nor geometry generates the other. No new primitive; canonical
AT unchanged.

**Reproduction:** `dotnet test AT.Tests/AT.Tests.csproj --filter "FullyQualifiedName~Y_QG_001"`

---

## References

- ResearchY-D_039 (Difference = distinguishability), M_005 (information conservation),
  NP_018 (distinguishability observable), NP_020 (black hole information), NP_021
  (information horizon).
- AT-QG: QG197 (metric ansatz g = ρ^(2/d)η), QG222 (native metric dynamics),
  QG228 (information I = KL(ρ‖uniform)), QG234 (ΩΛ = I_occ/ln K).
