# AT-QG Secondary-to-Natural Path Audit

**Status:** FUTURE PRE-REGISTRATION SCAN — SECONDARY OBSERVABLES ONLY

---

| Observable | Required preregistration | Confidence |
|---|---|---|
| `α_weak` | Pre-register as a ratio target fixed by the D96 coupling hierarchy before any comparison to the measured weak coupling. | Medium |
| `α_strong` | Pre-register as a ratio target fixed by the D96 coupling hierarchy before any comparison to QCD data. | Medium |
| `sin²θ_W` | Pre-register as a Weinberg-angle target from the coupling structure, with the target frozen before using electroweak data. | Medium |
| CKM matrix | Pre-register the quark-mixing structure and angles before comparing with the measured CKM catalog. | Medium |
| PMNS matrix | Pre-register the neutrino-mixing structure and angles before comparing with the measured PMNS catalog. | Medium |
| `S, T, U` | Pre-register the precision-observable triplet before using the EW fit, with no post-fit target selection. | Low |
| electron `g-2` | Pre-register the anomaly target before using the measured electron moment. | Low |
| `Σ√m` | Pre-register the half-moment as a structural access count before assigning any particle-sector label. | High |
| `Σm` | Pre-register the full-moment access role before assigning the charged-sector label. | High |
| `Σm²` | Pre-register the doublet-occupancy moment before assigning the doublet-sector label. | High |
| `occMom` | Pre-register the octave-occupation moment before assigning Higgs or cosmology labels. | High |
| neutrino masses | Pre-register absolute neutrino masses from the moment hierarchy before using oscillation data as target selection. | Medium |
| charged lepton hierarchy | Pre-register the hierarchy as a structural readout anchored only by `m_e`, before fitting charged-lepton targets. | Medium |
| quark masses | Pre-register the quark mass ratios from the spectral access structure before using the quark mass catalog. | Medium |
| weak scale `v` | Pre-register `v` as the electroweak calibration anchor before using `M_W`/`M_Z` targets. | Medium |
| `M_W` | Pre-register the weak-boson mass relation before comparison with the measured `W` mass. | Medium |
| `M_Z` | Pre-register the `Z`-mass relation before comparison with the measured `Z` mass. | Medium |
| `M_Pl` | Pre-register the Planck-scale amplification relation before using the physical Planck mass. | Medium |
| `G` (natural units) | Pre-register `G = 1/M_Pl²` after the Planck-scale relation is frozen, before unit conversion. | Medium |
| `G` (SI) | Pre-register only as a unit-conversion boundary once the natural-unit value is fixed. | Low |
| `Λ` | Pre-register the vacuum-pressure readout before using cosmological-constant data. | Low |
| `Ω_Λ`, `Ω_m` | Pre-register the occupancy-fraction split before using the observed density fractions. | Low |
| `n_s` | Pre-register the scalar tilt before any CMB spectral fit or peak alignment. | Low |
| CMB peaks (`ℓ_1`, `r_21`, `r_31`) | Pre-register the harmonic ratios before using the acoustic-peak catalog. | Low |
| gravitational redshift / time dilation / GPS | Pre-register the metric readouts before using the precision-gravity catalog. | Medium |

---

## Referee-Safe Conclusion

Most secondary observables could only move toward the natural class if their target existed as a frozen pre-registration before comparison and without parameter tuning. The highest-confidence upgrade paths are the moment observables, because their roles are structural; the lowest-confidence paths are the cosmology and acoustic targets, because they remain most exposed to retrospective matching.

