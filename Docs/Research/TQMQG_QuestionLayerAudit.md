# TQM-QG Phase 277 — Question Layer Audit

**Status:** COMPLETE — **QUESTION LAYER**
**Tests:** TQMQG2770, TQMQG2771, TQMQG2772 (all passed)
**Core class:** `TQM.Core/ResearchXH/QuestionLayerAudit.cs`
**Hypothesis:** measurement classes emerge from more fundamental question classes.
**Method:** no observables, no target values, D96 only, deterministic.

---

## 1. The Five Question Classes (one-to-one)

| Question | Asks for | Measurement class | Sector |
|----------|----------|-------------------|--------|
| how much? | a magnitude | VALUE | mass |
| how strong? | an interaction | STRENGTH | coupling |
| how oriented? | an alignment | ORIENTATION | mixing |
| how global? | the whole | GLOBAL | cosmology |
| what shape? | the geometry | GEOMETRY | gravity |

**Each question selects a DISTINCT measurement class** — the mapping is
one-to-one.

---

## 2. The Generative Structure

The question determines **what KIND of read is needed**:

```
'how much?'     → magnitude → value read (dimensional)
'how strong?'   → interaction → ratio read (dimensionless)
'how oriented?' → alignment → angle read (unitary)
'how global?'   → the whole → log read (scale-invariant)
'what shape?'   → geometry → power/deficit read
```

The question class **SELECTS** the measurement class.

**The question classes ARE the QG275 axis positions** (LEVEL × NATURE):
- how much? = (local, absolute) → VALUE
- how strong? = (local, relational) → STRENGTH
- how oriented? = (relational, frame) → ORIENTATION
- how global? = (global, relational) → GLOBAL
- what shape? = (arena, absolute) → GEOMETRY

---

## 3. The Question Layer

```
QUESTION (how much/strong/oriented/global/shape)
    → MEASUREMENT CLASS (value/strength/orientation/global/geometry)
    → EQUATION FORM (equality/ratio/unitary/log/power)
    → OBSERVABLE
```

The question is the **ORIGIN**: it selects what kind of read is needed, which
determines the class, the equation form, and the observable.

---

## 4. Conclusion

### **QUESTION LAYER** (question-layer score 6/6)

A structural question layer **generates** the measurement classes. The question
classes are the most primitive classification of **what is asked** about the
spectrum — the QG275 axis positions (LEVEL × NATURE).

**Honest caveat (QG275):** the question→sector completion (which sector a
"how strong?" read belongs to — coupling vs mixing vs mass-ratio) retains the
relational-subclass context-dependence. The QUESTION layer is the structural
origin; the SECTOR role is the residual.

**The reduction chain (QG260→277):**
```
Resonance Layer → Operator Layer → Same Operator Sectors
→ Single Resonance Dynamics → Single Resonance Invariant
→ Universal Conservation → Self-Consistency
→ Individuation → Difference Principle
→ Post-Resonance Integrity (frontier = assignment)
→ Sector Emergence → Partial Assignment → Measurement Class Layer
→ Partial Role Principle → Equation Class Layer
→ QUESTION LAYER (question → measurement class → equation form → observable)
```

The structure that generates all measurement classes is the **question layer**:
how much, how strong, how oriented, how global, what shape — the primitive
questions that select the kind of read the theory must produce.
