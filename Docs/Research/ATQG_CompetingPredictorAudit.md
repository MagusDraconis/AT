# AT-QG Phase 319 (reissue) — Competing Predictor Audit

**Status:** COMPLETE — **NO ADVANTAGE**
**Tests:** ATQG3190, ATQG3191, ATQG3192 (all passed)
**Core class:** `AT.Core/ResearchXH/CompetingPredictorAudit.cs`
**Question:** do the locks OUTPERFORM the standard complexity measures — entropy, gini, power-law exponent, spectral gap — at predicting the future HIGH organization class?
**Method:** deterministic, no observables, no target values — all five predictors use the SAME direction-aware top-third protocol over the 12-system evolving cohort (the QG317 family).

---

## 1. The Five Competing Predictors

All computed from the EARLY stage (25% growth) spectrum; the future HIGH class = top third by stage-8 maturity:

| Predictor | Correlation | Accuracy | Precision | Recall |
|---|---|---|---|---|
| entropy | −0.891 | 100% | 100% | 100% |
| gini | 0.967 | 100% | 100% | 100% |
| power-law exponent | 0.974 | 100% | 100% | 100% |
| spectral gap | 0.981 | 100% | 100% | 100% |
| **lock coherence** | 0.786 | **83%** | 75% | 75% |

## 2. The Protocol (identical for every predictor)

Direction-aware: if the predictor correlates positively with future maturity, the HIGH class is predicted as its top third; if negatively, its bottom third. This is the SAME top-third-class protocol as QG317, applied uniformly — no predictor gets a special rule.

## 3. The Result

**All four standard complexity measures reach 100% accuracy; the lock coherence reaches only 83%.**

- **gini, exponent, spectral gap** (positively correlated) and **entropy** (negatively correlated) all perfectly identify the future HIGH third;
- The lock coherence misses the future HIGH class 17% of the time (precision/recall 75%);
- QG317's 8/8 was NOT a lock-specific advantage — the standard measures match or beat it on the same cohort.

---

## 4. Conclusion

### **NO ADVANTAGE** (advantage score 4/5)

**The locks provide NO predictive advantage over the standard complexity measures on this cohort.**

- The lock coherence is a valid predictor (83% accuracy, 0.79 correlation), but every standard measure — entropy, gini, power-law exponent, spectral gap — reaches 100% on the evolving power-law cohort;
- QG317's 8/8 is explained by the cohort's structure (monotone in the exponent), not by a lock-specific power: the standard measures capture the same information at least as well;
- The lock identity's value lies in its ORIGIN (QG318: the moment-chain fixed point) and its D96-specific content (QG312-313), not in outperforming standard complexity measures as a generic predictor.

**The reduction chain (QG260→319):**
```
Resonance Layer → … → Blind Organization Prediction → False Positive Audit
→ Lock Origin Audit → COMPETING PREDICTOR AUDIT
(the locks provide NO ADVANTAGE over entropy, gini, exponent, and spectral gap on the evolving cohort)
```

**Frontier status:** the locks are a valid but non-superior predictor; their value is structural (QG318), not competitive. Remaining frontier unchanged: temporal evidence (SM), SM gaps (Bekenstein 1/4), ψ fundamental status, Difference boundary, methodology.
