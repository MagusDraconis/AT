# G4-F Phase 1 — Riemannian Reassessment

**Program:** G4 (with G4-T, G4-C, G4-D, G4-E, G4-F)
**Status:** COMPLETED — synthesis of 15 phases (45 deterministic xUnit tests); no new physics, no new primitives.
**Question:** Does TQM now contain a native Riemannian geometry program?

---

## 1. The chain, classified

| Chain element | Classification | Evidence |
|---|---|---|
| Q-events | **REAL-UNDERIVED** (primitive) | irreducible bedrock (QG-006/021) |
| event density ρ | **DERIVED** (concept); specific profile = contingent content | counting measure, native; G4-F0 (ρ ≡ 1 flat, varies 1.003→1.452 curved) |
| conformal factor f = ρ^(2/d) | **DERIVED** | MetricOriginClosure: native counting measure |
| native operator Lc = ρ⁻¹ L ρ⁻¹ | **DERIVED** (construction); analytic proof **OPEN** | G4-C0 (separation 3.12), G4-C1 (SC1–SC4), G4-C-Uniqueness |
| curvature reconstruction | **DERIVED** (sign+ordering); magnitude **OPEN** | G4-C2 (score −3.24/0/+4.34), G4-C3 (sign 10/10) |
| curvature dynamics | **DERIVED** (mean-field); field-level **OPEN** | G4-D0 (Pearson 0.98), G4-E0 (R=F(ρ), F′<0) |
| restoring dynamics | **DERIVED** (mean-field) | G4-E2 (d\* = 10.68; logistic bistable) |

---

## 2. (1) What is now solved?

- **The native operator.** Lc = ρ⁻¹ L ρ⁻¹ is built from the counting measure ρ and the causal
  adjacency alone; it reproduces Δ_g (SC1–SC4: sign separation, degree-artifact-free, 5/5
  observables monotonic, refinement-stable) and is the **unique conformal Laplace–Beltrami
  representative** of its family.
- **Curvature sign and ordering** are recovered from Lc spectra.
- **Mean-field curvature dynamics**: a closed evolution law R = F(ρ), Ṙ = F′(ρ)·ρ̇, F′ < 0;
  feedback characterized (anti-diffusive) and stabilizable (diffusion/logistic).
- **The meaning of ρ**: the counting measure (event density ≡ actualization rate), 0 new primitives.

## 3. (2) What remains imported?

- **The conformal class** (causal order → light-cone structure → conformal class) — imported
  (Malament 1977 / HKM 1976). It is a **proven theorem**, not a theory gap; only the *factor*
  (ρ side) is native.
- **Δ_g (Laplace–Beltrami formula)** — used as the **benchmark** (C4 role) to validate Lc, never
  as a construction input. The construction is native; the *validation* is imported.
- **BDG binomial weights** (Lorentzian sector) — untouched by G4.

## 4. (3) What changed relative to the v1.0 monograph?

| | v1.0 monograph | after G4 (v1.1) |
|---|---|---|
| Operator (Riemannian) | **imported** (weighted Laplacian L_W over spatial coupling K) | **native** (Lc = ρ⁻¹Lρ⁻¹) |
| Metric | natively determined (class × ρ^(2/d)) | unchanged (confirmed by G4-F) |
| Curvature read-out | not established | sign + ordering reconstructed from Lc spectra |
| Operator→coupling gap | listed as missing/open ("Einstein coupling (G4) still missing") | **MOSTLY CLOSED** in the Riemannian sector |

## 5. (4) The original G4 blocker

**MOSTLY CLOSED.**

- **Closed (Riemannian/conformal):** native operator, curvature sign+ordering, mean-field
  dynamics, ρ interpretation.
- **Open:** Lorentzian d'Alembertian (BDG weights), absolute curvature magnitude, field-level
  dynamics, analytic continuum proof, diffusion-generator closure.

---

## 6. Overall verdict

**Yes** — TQM now contains a **native Riemannian geometry program** in the conformally-flat
sector. The chain `Q-events → ρ → f → Lc → R → dynamics → restoring` is DERIVED end-to-end at
the level of structure, with only two imports remaining: the conformal **class** (a proven
theorem) and the Δ_g **benchmark** (used for validation, not construction). The original G4
blocker is **MOSTLY CLOSED** — the operator is no longer imported; it is the native
counting-measure-weighted operator that the metric's own conformal factor selects.
