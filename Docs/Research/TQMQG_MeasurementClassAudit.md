# TQM-QG Phase 274 — Measurement Class Audit

**Status:** COMPLETE — **MEASUREMENT CLASS LAYER**
**Tests:** TQMQG2740, TQMQG2741, TQMQG2742 (all passed)
**Core class:** `TQM.Core/ResearchXH/MeasurementClassAudit.cs`
**Hypothesis:** physics sectors are not fundamental — measurement classes exist between projections and sectors.
**Method:** no observables, no target values, D96 only, deterministic.

---

## 1. The Five Measurement Classes (all structurally unique)

| Class | Structural signature | Determined by form? | Dominant sector |
|-------|----------------------|---------------------|-----------------|
| **VALUE** | dimensional absolute magnitude (me-anchored) | ✓ | mass |
| **STRENGTH** | normalized dimensionless ratio | ✓ | coupling |
| **ORIENTATION** | unitary arrangement (V†V = I) | ✓ | mixing |
| **GLOBAL** | scale-invariant log-of-spectrum | ✓ | cosmology |
| **GEOMETRY** | power≥2 combination / deficit | ✓ | gravity |

**5/5 classes are structurally unique** — determinable by form, in contrast to
sectors (QG273: a ratio read is ambiguously coupling, mixing, or mass-ratio).

---

## 2. The QG273 Ambiguity Is Resolved

QG273 found the identical form √occMom·λ₂ is assigned to both sectors:

```
m_τ/m_μ (mass)   = √occMom·λ₂ = 16.842
y_τ/y_μ (coupling) = √occMom·λ₂ = 16.842
```

Through the **measurement-class layer** this is **RESOLVED**: √occMom·λ₂ is
**unambiguously a STRENGTH read** (a dimensionless ratio of D96 quantities).
Its *class* is structural and unique; only its *sector role* (mass hierarchy vs
Yukawa Lagrangian) is assigned by the equation it enters.

The QG273 ambiguity was in the **sector labels**, not in the measurement class.

---

## 3. The Layer Structure

```
PROJECTIONS (operator outputs)
    → MEASUREMENT CLASSES (value/strength/orientation/global/geometry — structurally unique)
    → SECTORS (mass/coupling/mixing/cosmology/gravity — role labels)
```

- Each class maps to a **distinct dominant sector**.
- The **class is structural** (determined by the read's form).
- The **sector is role** (which question the read answers in the theory).
- Strength reads span multiple sectors (coupling, mixing, mass-ratio) — the
  structural class is stable across the roles.

---

## 4. Conclusion

### **MEASUREMENT CLASS LAYER** (class-layer score 6/6)

A structurally determinable measurement-class layer exists **between projections
and sectors**. The sector labels **EMERGE from the measurement classes**:

- the class is determined by the read's form (value/ratio/orientation/log/power);
- the sector is the role that read plays in the theory.

This **resolves the QG273 ratio ambiguity**: the ambiguous read √occMom·λ₂ is
unambiguously a strength read — the ambiguity was in the sector *labels*, not
the measurement *class*.

**The reduction chain (QG260→274):**
```
Resonance Layer → Operator Layer → Same Operator Sectors
→ Single Resonance Dynamics → Single Resonance Invariant
→ Universal Conservation → Self-Consistency
→ Individuation → Difference Principle
→ Post-Resonance Integrity (frontier = assignment)
→ Sector Emergence (sectors = projection classes)
→ Partial Assignment (4 structural rules + role-based ratio-class)
→ MEASUREMENT CLASS LAYER (classes are structural; sectors are roles over them)
```

**Where this leaves the frontier:** the assignment step is now located precisely —
the **measurement class** of every read is structural (derived from form), and
the residual step is the **class → sector role** assignment (which equation a
strength read enters: mass hierarchy vs Yukawa). The class layer is the
structural skeleton; the role mapping is the remaining target-informed step.
