# AT-QG Moment-Sector Assignment Audit

**Status:** REFEREE AUDIT — STRUCTURAL CORE FORCED, SECTOR LABELS SUPPORTED BUT NOT GLOBALLY UNIQUE

---

## Executive Summary

**Forced:**
- `Σ√m =` neutral access
- `Σm =` full access
- `Σm² =` doublet occupancy access
- `occMom =` octave occupancy access

**Supported:**
- neutrino ↔ `Σ√m`
- charged sector ↔ `Σm`
- doublet sector ↔ `Σm²`
- Higgs/cosmology ↔ `occMom`

**Not Proven:**
- uniqueness of sector assignment
- impossibility of alternative labels

---

## Detailed Assignment Table

| Assignment | Reason | Status | Referee confidence |
|---|---|---|---|
| `Σ√m → neutrino` | The documents identify `Σ√m` as the half-moment / neutral access count. It is then used in the absolute neutrino mass laws and neutrino splittings, so the neutrino association is structurally motivated but still anchored to the downstream mass fit. | Supported | Medium |
| `Σm → charged sector` | `Σm` is explicitly the first moment / full access / total mode count. That makes it the natural structural carrier for the charged sector, but the named sector label is still an assignment on top of the forced full-access role. | Supported | Medium-high |
| `Σm² → doublet sector` | `Σm²` is the second moment / doublet-occupancy access count. The moment order is mathematically distinguished and the doublet interpretation is directly stated in the source material, but the sector label remains a reconstruction choice rather than a uniqueness theorem. | Supported | Medium-high |
| `occMom → Higgs` | `occMom` is the octave-occupation moment / dense-band access count. It is repeatedly used in Higgs-related constructions, so the assignment is well supported by the reconstruction program. | Supported | Medium |
| `occMom → cosmology` | The same octave-occupation structure is reused in cosmological fractions and sector-readout claims. The mapping is consistent with the monograph’s architecture, but not shown to be the only possible labeling. | Supported | Medium |
| `Σ√m → neutrino sector only` | No global proof excludes other labels that might also be compatible with the half-moment structure. The current material shows the neutrino mapping is the best-supported one, not the unique one. | Not Proven | Medium |
| `Σm → all charged observables uniquely` | The first moment is structurally forced, but its downstream interpretation across charged observables is not shown to be label-unique under all possible operator-basis reformulations. | Not Proven | Medium |
| `Σm² → only doublet sector possible` | The second moment is forced as a structural count, but the claim that no alternative physical labeling could reproduce the same second-moment role is not proved globally. | Not Proven | Medium |
| `occMom → only Higgs/cosmology labels possible` | The octave-occupation moment is forced, but the named Higgs/cosmology labels are still phenomenological readouts, not an impossibility proof against alternatives. | Not Proven | Medium |

---

## Referee-Safe Conclusion

The moment hierarchy is structurally forced.
The mapping to named physical sectors remains
supported but not globally unique.

