namespace TQM.Core.ResearchXC;

/// <summary>
/// Founding analysis: Why does reality split into Identity and Abundance layers?
/// ResearchXC-001: Origin of the Identity-Abundance Split
/// </summary>
public static class IdentityAbundanceSplitAnalyzer
{
    public static string TheSplitOrigin()
    {
        return @"
RESEARCHXC — UNIFICATION PHYSICS

THE FUNDAMENTAL QUESTION:

  Why does TQM have TWO explanatory layers?

    IDENTITY:   Topology → What exists (discrete, structural).
    ABUNDANCE:  History → How much (continuous, statistical).

  Why not ONE layer? Why this specific split?

THE ANSWER: The split IS the two-primitive structure of TQM.

  Q (individuation):
    Provides distinguishability — the graph, moduli spaces,
    topological invariants. These are DISCRETE and INVARIANT
    under continuous change → IDENTITY LAYER.

  RANDOMNESS (actualization):
    Selects one outcome from the Born distribution. Each
    selection is contingent. Accumulated selections build
    up HISTORY → ABUNDANCE LAYER.

  THE SPLIT IS NOT A MYSTERY — IT IS THE DEFINITION OF TQM.

  Q → topology → discrete invariants → 'what exists'.
  Randomness → statistics → accumulated history → 'how much'.

THE MATHEMATICAL DISTINCTION:

  Identity quantities = TOPOLOGICAL INVARIANTS:
    • Invariant under continuous deformations of history.
    • Determined by the ERGODIC AVERAGE over all possible histories.
    • Depend only on Q (the space of possibilities).

  Abundance quantities = PATH-DEPENDENT ACCUMULATIONS:
    • Change with each actualization event.
    • Depend on the SPECIFIC HISTORY of actualizations.
    • Their DISTRIBUTION (not value) is determined by Q + randomness.

  This is exactly the split between:
    • The space of possibilities (Q → identity).
    • The realized trajectory (randomness → abundance).

WHY BOTH ARE NEEDED:

  Without identity: no structure, no entities, no laws.
  Without abundance: no specific values, no measurements.
  Both are necessary for a complete description of reality.

CLASSIFICATION D: The split is DERIVED from the TQM primitives.
  Q = topology = identity. Randomness = history = abundance.
  The split IS TQM. No deeper principle is needed.
";
    }

    public static string TheTwoLayerOntology()
    {
        return @"
THE COMPLETE TQM ONTOLOGY

═══════════════════════════════════════════════════════════════
  PRIMITIVE 1: Q — INDIVIDUATION → IDENTITY LAYER
═══════════════════════════════════════════════════════════════

  Q provides:
    • Distinguishable entities (the graph).
    • Topological invariants (Betti numbers, homotopy groups).
    • Moduli spaces (spaces of equivalent configurations).
    • Symmetries (automorphism groups of moduli spaces).

  IDENTITY QUESTIONS ANSWERED:
    ✓ What particles exist?    (topological defect types)
    ✓ What gauge groups?       (Aut of moduli spaces)
    ✓ Why 3 generations?       (stability cutoff — integer)
    ✓ Why 3+1 dimensions?      (complexity optimum — integer)
    ✓ Why U(1)?                (S¹ moduli → Aut = U(1) — theorem)

  Identity = TOPOLOGY.
  Discrete. Invariant. Universal.

═══════════════════════════════════════════════════════════════
  PRIMITIVE 2: RANDOMNESS — ACTUALIZATION → ABUNDANCE LAYER
═══════════════════════════════════════════════════════════════

  Randomness provides:
    • Outcome selection (Born rule → which branch).
    • Time (partial order of actualizations).
    • History (accumulated actualization record).
    • Statistics (distributions over ensembles).

  ABUNDANCE QUESTIONS ANSWERED:
    ✓ Distribution family?     (log-normal from cascades)
    ✓ Variance origin?         (σ² = N·σ₀², cascade depth)
    ✓ Volatility origin?       (σ₀² = Var[-log(p)], Born rule)
    ✓ Mean drift?              (μ = log(N_f/N_i), expansion)
    ✓ Freezeout?               (Γ_X(T_f) = H(T_f))

  Abundance = HISTORY.
  Continuous. Statistical. Contingent.

═══════════════════════════════════════════════════════════════
  UNIFIED BY M²:
    The single continuous parameter governs BOTH layers.
    M² sets the defect potential (identity → particle spectrum).
    M² sets the actualization volatility (abundance → distributions).
═══════════════════════════════════════════════════════════════
";
    }
}
