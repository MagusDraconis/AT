namespace TQM.Core.ResearchXB;

/// <summary>
/// Founding analysis for ResearchXB: Abundance Physics.
/// ResearchXB-001: Origin of Abundance
/// </summary>
public static class AbundanceOriginAnalyzer
{
    public static string AbundanceDefinition()
    {
        return @"
RESEARCHXB — ABUNDANCE PHYSICS

FOUNDING INSIGHT (TQM-X065b):

  TQM has TWO fundamentally different explanatory categories:

  IDENTITY (93% derived):
    'What exists?' — answered by TOPOLOGY.
    Discrete properties: particle species, gauge groups, dimensions.
    Topological invariants determine identity.

  ABUNDANCE (14% derived):
    'How much exists?' — answered by HISTORY.
    Continuous quantities: masses, couplings, densities.
    Initial conditions and cosmological evolution determine abundance.

WHY THIS MATTERS:

  The Standard Model treats ALL parameters as fundamental — confusing
  identity (which could be derived) with abundance (which cannot).

  TQM SEPARATES them, explaining why some questions are answerable
  (topology) and others require measurement (history).

RESEARCHXB MISSION:

  Investigate whether abundance quantities share a COMMON ORIGIN
  in cosmological history, actualization statistics, or ensemble
  probabilities — rather than being independent parameters.
";
    }

    public static string TheUniverseEnsemble()
    {
        return @"
THE UNIVERSE ENSEMBLE — IDENTITY × HISTORY

Fix identity (topology: Q, gauge groups, particle species).
Vary history (initial conditions: defect density, temperature).

Each universe in the ensemble has:
  • SAME identity (same particles, same forces, same laws).
  • DIFFERENT abundance (different masses, couplings, densities).

Our universe is ONE SAMPLE from this ensemble.

PREDICTION: Abundance quantities should follow PROBABILITY
DISTRIBUTIONS determined by the actualization process, not
fixed values.

This is analogous to:
  • Statistical mechanics: microstate is random, macrostate fixed.
  • Quantum mechanics: outcome is random, probability is fixed.
  • TQM abundance: value is random, DISTRIBUTION may be derivable.
";
    }

    public static string TheMinimalAbundanceTheory()
    {
        return @"
MINIMAL THEORY OF ABUNDANCE

All abundance quantities = FROZEN INITIAL CONDITIONS
from the early-universe phase transition epoch.

  1. MASS SCALE (m_e, xi): set by defect density at EWPT.
  2. COUPLINGS (alpha): set by vortex geometry at formation.
  3. RELIC DENSITIES (Omega): set by initial defect population.
  4. NONLINEARITY (M^2): coarse-grained from Q-event statistics.

They are NOT fundamental constants.
They are HISTORICAL ACCIDENTS.
Our universe's particular values, frozen at the electroweak scale.
";
    }
}
